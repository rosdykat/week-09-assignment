// TODO: include the user's personal details
// auth() --> {userId}
// currentUser() --> firstName, lastName, emailAddresses[0].emailAddress

export default function UserProfile() {
  return (
    <>
      <h1>User Profile Page</h1>
      {/* render user's data here */}
    </>
  );
}
